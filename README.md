• This project was developed in Eclipse IDE for Java Developers (4.15.0 version);
• It was used MVP as software pattern;
• It was missing to handle the pilots positions that didn't complete at least 4 laps;
• The race data was compiled into table in database (race.db);